# Multiverse runtime dependencies.
# Install with: pip install -r requirements.txt
#
# Optional layers:
#   pip install -r requirements_ui.txt     # Local viewer/front-end extras
#   pip install -r requirements_test.txt   # Pytest and test runners
#   pip install -r requirements_scale.txt  # Distributed/scaling features

# Core ML/RL framework
numpy>=1.24.0,<2.0.0
torch>=2.0.0
scikit-learn>=1.3.0
scipy>=1.10.0

# Type system and validation
pydantic>=2.0.0
typing-extensions>=4.5.0

# Configuration and environment
python-dotenv>=1.0.0
PyYAML>=6.0.0
watchdog>=3.0.0

# HTTP/networking surfaces
requests>=2.31.0
httpx>=0.24.0

# Memory/vector runtime support
faiss-cpu>=1.7.4
psutil>=5.9.0

# Notes:
# - Python 3.10+ required (tested on 3.12)
# - GPU support requires a torch build with CUDA from pytorch.org
